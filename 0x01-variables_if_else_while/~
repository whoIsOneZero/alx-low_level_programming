#include<stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		if ((alpha == 65) || (alpha == 71))
			continue;
		else
			putchar(alpha);
		printf("%d", alpha);
	}
	putchar('\n');
	return (0);
}
