#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: the paramenter
 *
 * Return: string length (int)
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
